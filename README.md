# News bot

Telegram-бот публикует криптоновости в канал и собирает карточку поста с картинкой.

Стек: Python, aiohttp, BeautifulSoup, Pillow.

Токен бота задаётся в локальном `.env`, в репозиторий не входит. Пример — `.env.example`.
